import styles from "./TodoInfoHelper.style.module.scss";

const TodoInfoHelper = () => {
  return (
    <div className={styles.todoInfoHelper}>Drag and drop to reorder list</div>
  );
};

export default TodoInfoHelper;
